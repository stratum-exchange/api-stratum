version: "3.9"

services:
  redis:
    image: "redis:alpine3.16"
    command: redis-server /usr/local/etc/redis/custom_redis.conf
    ports:
      - "6379:6379"
    volumes:
      - ./custom_redis.conf:/usr/local/etc/redis/custom_redis.conf
    networks:
      - backend

  app:
    build:
      context: .
    expose:
      - "3033"
    volumes:
      - ./:/app
      - /etc/ssl/app.stratumexchange.com:/etc/ssl/app.stratumexchange.com
    depends_on:
      - redis
    networks:
      - backend
    env_file:
      - .env
    environment:
      REDIS_HOST: redis

networks:
  backend:
